SOME PYTHON EXERSISES TO TEST YOUR SKILLS
----------------------------------------------------------

.. image:: https://img.shields.io/pypi/pyversions/pyjokes
   :target: https://python.org/
   :alt: Supported Versions.

1. Write a program to print "Hello World".
2. Write a program assign values to 2 variables and print their sum.
3. Write a program to ask the user for some value and print them.
4. Write a program to print a multiline string with only one print function.
5. Write a program to ask the user for a number and print 'The number entered was _number_'. 
6. Write a program to add 2 numbers.
7. Write a program to subtract 2 numbers.
8. Write a program to multiply 2 numbers.
9. Write a program to divide 2 numbers.
10. Do Ex5, Ex6, and Ex7 and Ex8 but take the numbers as an input from the user.
11. Write a program to input a number and check if it is even or odd.
12. Write a program to input a number and check if it is odd or even multiple of 7. If it is not one, print that it is not.
13. Write a program to input a number print all the factors of it.
14. Write a program to count to 100.
15. Write a program to print all even numbers from 1 to 100.
16. Write a program to print all odd numbers from 1 to 100.
17. Write a program to print all prime numbers from 1 to 100.
18. Write a program to input a single character and print weather it is a vowel, consonant, digit or a special character.
19. Write a program print square root of a number.
20. Write a program to find the perimeter and area of a circle from the input radius.
21. Write a program to find the perimeter and area of a square from the input length.
22. Write a program to convert 400 seconds to minutes and seconds.
23. Write a program to input seconds and convert seconds to minutes and hours.
24. Write a program to find roots of a quadratic equation.
25. Write a program and define a function that adds/subtracts/multiplies/divides 2 numbers.
26. Write a program that stores every input and on a specific keyword shows all previous values.
27. Write a program to create a local directory './py_directory'
28. Write a program to count to 10 but with a delay of 1 second after each count.
29. Write a program to calculate intrest from on principle amount and time input from the user and print total amount. (Rate of interest = 10%)
30. Write a program to save an input into a json file.
31. Write a program to create a calculator class and define addition, subtraction , multiplication and division fuctions in it.
32. Write a program to input a number and print the pattern that many times. 

   *

   * *

   * * *

33. Write a program to input a number and print a new number with the odd digits first and the even digits later.
34. Write a program to tell if an input number is an armstrong number
35. Write a program to count the frequency of each character in an input string.
36. Write a program to check if an input string is a palindrome.
37. Write a program to capitalize the first letter of every word in a sentence without using the built-in `title()` method.
38. Write a program to find the longest word in a given sentence.
39. Write a program to remove all duplicate characters from a string while preserving the original order.
40. Write a program to find the second largest element in a list of numbers entered by the user.
41. Write a program to rotate a list of elements by $k$ positions to the right. *(Note: use standard text variables instead of LaTeX formatting for simple parameters).*
42. Write a program to merge two sorted lists into a single sorted list without using built-in sort functions.
43. Write a program to find the intersection and union of two lists.
44. Write a program to count the occurrences of each word in a text file.
45. Write a program to transpose a matrix (2D list) entered by the user.
46. Write a recursive function to calculate the factorial of a number.
47. Write a recursive function to generate the n-th Fibonacci number.
48. Write a recursive function to check if a string is a palindrome.
49. Write a function that accepts a variable number of positional arguments and returns their average.
50. Write a program using `lambda` and `map` to square all elements in a list.
51. Write a program using `filter` to extract all prime numbers from a list of integers.
52. Write a generator function that yields powers of 2 up to $n$ terms.
53. Write a function that uses `functools.reduce` to find the product of all elements in a list.
54. Write a decorator function that measures and prints the execution time of any function it wraps.
55. Write a decorator function that logs the arguments and return value of a function every time it is called.
56. Write a program to read a text file and count the total number of lines, words, and characters.
57. Write a program that safely handles `FileNotFoundError` when attempting to open a user-specified file.
58. Write a program to append a new log entry with a timestamp to an existing file.
59. Write a program using the `random` module to simulate rolling two six-sided dice 1,000 times and display the frequency of each sum.
60. Write a program using the `datetime` module to calculate the exact number of days remaining until the user's next birthday.
61. Write a program using the `os` and `shutil` modules to back up all text files in a directory into a new backup folder.
62. Write a program to read data from a CSV file and display it in a formatted tabular layout.
63. Write a program to write a list of dictionaries into a CSV file with proper column headers.
64. Write a program using `urllib` or `requests` to fetch and parse the status code of a user-provided URL with exception handling.
65. Write a program that validates a user-entered email address using regular expressions (`re` module).
66. Write a program to create a `BankAccount` class with methods for deposit, withdrawal, and balance inquiry, including custom error handling for insufficient funds.
67. Write a program to create a base `Shape` class and derived `Rectangle` and `Circle` classes that override an area calculation method.
68. Write a program featuring a `Book` class that overloads the `__str__` dunder method to return a readable book summary.
69. Write a program implementing a `Student` class with class attributes to track the total number of student instances created.
70. Write a program to create a custom exception `InvalidAgeError` and raise it if a user inputs an age less than 18.
71. Write a program implementing a `Vector` class that overloads the addition operator using `__add__` to combine two 2D vectors.
72. Write a program to create an employee management system using encapsulation with private attributes and getter/setter methods.
73. Write a program implementing a `Library` class that manages a collection of books using object composition.
74. Write a program to implement a basic shopping cart class utilizing property decorators for automatic total price calculation.
